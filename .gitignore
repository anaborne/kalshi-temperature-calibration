# private working notes, kept local
HANDOFF.md
VERIFICATION-RULES.md
CANDIDATES.md

# data cache (regenerated by the fetch_*.py steps)
data/*
!data/station_proposal.json

.venv/
__pycache__/
*.pyc
.DS_Store

# figures/ is committed: calibration.json is the diagnostic's own output and
# calibration.svg is rendered from it by make_figures.py, no deps required.
